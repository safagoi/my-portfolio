---
import Header from '../components/Header.astro';

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<Header />		
		<slot />
	</body>
</html>
<style is:global lang="scss">

	:root {
		--accent: 124, 58, 237;
		--accent-gradient: linear-gradient(45deg, rgb(var(--accent)), #da62c4 30%, white 60%);
	}

	* {
		margin: 0;
		padding: 0;
		box-sizing: content-box;
	}

	html, 
	body  {
		font-family: "Rubik";
		height: 100%;
		width: 100%;
	}

	/* Layout */
	body {
		display: grid;
		grid-template-rows: 10rem 1fr;
		background-color: #F5F5DC;
		color:  #232222;
	}

	button {
		/* Button component styles */
		padding: 0.45rem 0.75rem;
		min-width: 5rem;
		border: none;
		background-color: #ffbf00;
		color: #232222;
		border-radius: 5px;
		cursor: pointer;
		font-weight: 600;
		box-shadow: 0px 1px 1px rgba(0, 0, 0, 0.387);
		transition: transform 0.3s ease-out;

		&:hover {
			background-color: #ffcd36;
			transform: translateY(-2px);
		}
	}

	.button-icon {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0rem .75rem;
	}
	
	/* $color-fondo-cuaderno-antiguo: #F5F5DC;
	$color-texto-cuaderno-antiguo: #000000;
	$color-beige-cuaderno-antiguo: #F5F5DC;
	$color-marron-cuaderno-antiguo: #704214;
	$color-verde-cuaderno-antiguo: #8F9779;
	$color-amarillo-cuaderno-antiguo: #D2B55B;
	$color-gris-cuaderno-antiguo: #D3D3D3; */
</style>
